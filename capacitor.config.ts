import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kyle.todonative',
  appName: 'todo-native',
  webDir: 'dist/todo-native/browser'
};

export default config;
